{
  "name": "bijak-mengeluh-scripts",
  "version": "1.0.0",
  "description": "Data collection and enrichment scripts",
  "scripts": {
    "enrich": "ts-node enrich-agency-data.ts",
    "import-csv": "ts-node import-from-csv.ts",
    "validate": "ts-node validate-agency-data.ts"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0",
    "csv-parse": "^5.5.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}
